{
  "name": "reports",
  "version": "0.1.0",
  "description": "Interactive app to get reports for the guyveut bot",
  "author": "Jean-Brice Gachot",
  "license": "MIT",
  "main": "handler.js",
  "scripts": {
    "local": "serverless offline start",
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "deploy:all": "yarn run deploy && yarn run deploy:prod",
    "delete": "serverless remove",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@guyveut/shared": "^0.1.0",
    "@slack/events-api": "^3.0.1",
    "@slack/interactive-messages": "^2.0.2",
    "@slack/web-api": "^7.0.4",
    "serverless": "^3.38.0",
    "serverless-prune-plugin": "^2.0.2"
  },
  "devDependencies": {
    "serverless-offline": "^13.3.4"
  }
}
