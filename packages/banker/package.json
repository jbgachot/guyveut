{
  "name": "banker",
  "version": "0.1.0",
  "description": "Banking handler for the guyveut bot",
  "author": "Jean-Brice Gachot",
  "license": "MIT",
  "main": "handler.js",
  "scripts": {
    "local": "serverless offline",
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "deploy:all": "yarn run deploy && yarn run deploy:prod",
    "delete": "serverless remove",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@guyveut/shared": "^0.1.0",
    "@slack/web-api": "^5.9.0",
    "jest": "^26.0.1",
    "serverless": "^3.38.0",
    "serverless-prune-plugin": "^2.0.2"
  },
  "devDependencies": {
    "serverless-offline": "^13.3.4"
  }
}
